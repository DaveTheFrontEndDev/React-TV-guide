import React, { Component } from 'react';
import Header from '../header';

class Home extends Component {
  render() {
    return (
      <Header>
        <h1>Please select a Show...</h1>
      </Header>
    );
  }
}

export default Home;
